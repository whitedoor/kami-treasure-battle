<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Kami Treasure Battle" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Kami Treasure Battle">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="ktb-topbar">
      <div class="ktb-topbar__inner">
        <div class="ktb-topbar__brand">
          <%= link_to "Kami Treasure Battle", root_path, style: "color: inherit; text-decoration: none;" %>
        </div>

        <div class="ktb-topbar__nav">
          <% if logged_in? %>
            <%= link_to "レシート作成", new_receipt_path, class: "ktb-pill" %>
            <%= link_to "保持カード", owned_cards_path, class: "ktb-pill" %>
            <%= button_to "ログアウト", logout_path, method: :delete, class: "ktb-pill", form: { style: "display:inline;" } %>
          <% else %>
            <%= link_to "アカウント作成", new_user_path, class: "ktb-pill" %>
            <%= link_to "ログイン", new_session_path, class: "ktb-pill" %>
          <% end %>
        </div>
      </div>
    </div>

    <% if flash[:notice].present? %>
      <div class="ktb-flash"><%= flash[:notice] %></div>
    <% end %>
    <% if flash[:alert].present? %>
      <div class="ktb-flash ktb-flash--alert"><%= flash[:alert] %></div>
    <% end %>

    <%= yield %>
  </body>
</html>
