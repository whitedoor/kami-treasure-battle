## Optional environment variables for local development (Docker Compose)
##
## Copy to .env and fill what you need:
##   cp .env.example .env
##
## You can boot the Rails app without these. They are only needed for
## the receipt upload + Gemini extraction flow.
##
## Receipt upload (Google Cloud Storage)
# GCS_BUCKET=ktb-uploads
# GCP_PROJECT_ID=your-gcp-project-id
#
## If you use a service account key file (inside container),
## mount it and point GOOGLE_APPLICATION_CREDENTIALS to the container path.
# GOOGLE_APPLICATION_CREDENTIALS=/app/tmp/gcp-key.json
#
## Gemini extraction (API key mode; recommended for local dev)
# GEMINI_AUTH_MODE=api_key
# GEMINI_API_KEY=your-gemini-api-key
# GEMINI_MODEL=gemini-2.0-flash-001
#
## Gemini extraction (Vertex AI mode; requires ADC / workload identity)
# GEMINI_AUTH_MODE=vertex
# GEMINI_VERTEX_LOCATION=us-central1
#
